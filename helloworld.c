#include <stdio.h> // Include header file for standard input/output.

int main() // The "main()" function is where to program will start to execute.
{
	
	printf("Hello, world!\n"); // Display "Hello, world!" to the terminal.
				   // "\n" means newline.
	
	return 0; // Tells the OS that the program was successful.
	
}
